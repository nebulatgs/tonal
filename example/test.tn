import { trumpet, piano } from "std/instruments"

meta {
    title("Test")
    composer("me")
}

staff t is treble(trumpet) in [3/4] {
    pickup {
        quarter("C") with dot with tremelo
    }
    bpm(100)
    measure {
        quarter("C") with dot
    }
}